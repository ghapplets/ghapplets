### WIP

To develop locally, you will need `node` and `npm i -g pug`. Then simply `npm run watch` and edit files. Changes will reload automatically under `localhost:3000`.

To make changes and commit them, simply run `npm publish` and `git add .`.
